/**
 * Summarizes two passed numbers.
 *
 * @param a first number
 * @param b second number
 * @returns sum of first and second numbers
 */
const myFunction = (a: number, b: number) => {
  return a + b;
};
